services:
  bsky-list-feeds:
    image: ghcr.io/npmanos/list-feeds:latest
    container_name: bsky-list-feeds
    restart: unless-stopped
    ports:
      - "7474:7474"
    volumes:
      - ./data:/data
      - /etc/localtime:/etc/localtime:ro